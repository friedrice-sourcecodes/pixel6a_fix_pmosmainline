# Keep AoC out of the automatic cold-boot path until it has been validated.
# An explicit `modprobe aoc_core` still loads the module.
blacklist aoc_core
blacklist aoc_alsa_dev_util
blacklist aoc_alsa_dev

