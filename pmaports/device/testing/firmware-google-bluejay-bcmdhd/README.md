# Required local payloads

This package recipe expects `bcmdhd.ko`, `fw_bcmdhd.bin`, `bcmdhd.cal` and
`bcmdhd_clm.blob`. They are proprietary/device-derived files and are not part of
this source repository. Obtain them legally and verify them against the APKBUILD
checksums before building.

