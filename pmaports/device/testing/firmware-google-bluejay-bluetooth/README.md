# Required local payload

This package recipe expects the Bluejay BCM4389 firmware file `BCM.hcd`. It is
proprietary and is not part of this source repository. Obtain it legally and
verify it against the APKBUILD checksum before building.

